# Optional: only if the client is served from a different host than the app.
# When using the built-in custom server, leave unset to use the same origin.
# NEXT_PUBLIC_SIGNALING_URL=http://localhost:3000
