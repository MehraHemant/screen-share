# Required when deploying the frontend to Vercel (Vercel cannot run WebSockets).
# Deploy the signaling server (see /signaling) to Railway, Fly.io, or Render, then set:
# NEXT_PUBLIC_SIGNALING_URL=https://your-signaling.railway.app
# Local dev with custom server: leave unset (uses same origin).
