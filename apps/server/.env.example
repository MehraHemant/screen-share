# Port for the signaling server (default: 3001)
PORT=3001

# Allowed origin for Socket.io CORS. Use your frontend URL in production.
# Example: https://your-app.vercel.app
CORS_ORIGIN=*

# Optional: max viewers per room (omit for no limit)
# MAX_VIEWERS_PER_ROOM=50

# Optional: HTTPS (required when the web app is served over HTTPS to avoid mixed-content)
# USE_HTTPS=true
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem
# (e.g. use mkcert for local certs: mkcert -install && mkcert localhost)
